[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[blobs]]
  name = "mp_tokens"

# 🚀 Scheduler: ejecuta la función recordatorios cada 15 minutos
[[scheduled.functions]]
  name = "recordatorios"
  cron = "*/15 * * * *"
