[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[[blobs]]
  name = "mp_tokens"

# 🚀 Scheduler para recordatorios (cada 15 minutos)
[functions."recordatorios"]
  schedule = "*/15 * * * *"

# 🕒 Scheduler para actualizar días de prueba (1 vez al día)
[functions."actualizar-trials"]
  schedule = "0 6 * * *" # todos los días a las 06:00 UTC aprox

# ----------------------------
# ✅ URL nueva: /:slug
# ✅ URL vieja: /agenda/:slug  -> redirige a /:slug
# ----------------------------

[[redirects]]
  from = "/agenda/:slug"
  to = "/:slug"
  status = 301
  force = true

# ✅ SPA fallback (Vite): cualquier ruta carga la app
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
